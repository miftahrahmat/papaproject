// Initialization for ES Users
import {
    Popconfirm,
    Collapse,
    Dropdown,
    Validation,
    Input,
    Animate,
    Ripple,
    Offcanvas,
    Carousel,
    Toast,
    Rating,
    Select,
    Tab,
    initTE,
    Alert,
  } from "tw-elements";
  
  initTE({ Alert, Tab, Select, Toast, Validation, Animate, Offcanvas, Collapse, Input, Ripple, Dropdown, Carousel, Rating, Popconfirm });

import jQuery from 'jquery';
window.$ = jQuery;
  
import swal from 'sweetalert2';
window.Swal = swal;


